import React from 'react';
import PropTypes from 'prop-types';
import { ScrollView, View, Text, StyleSheet } from 'react-native';


export default function Drawer(props) {
  return (
    <ScrollView><Text>Drawer</Text></ScrollView>
  );
}

Drawer.propTypes = {
  
};
